#include <iostream>

using namespace std;

int main() {
    // 1. Деление числа на разряды
    // string num;
    // cout << "Enter number: ";
    // cin >> num;
    
    // for (int i = 0; i < num.length(); ++i) {
    //     cout << "num " << (i + 1) << " is: " << num[i] << endl;
    // }

    // 2. Запрограммировать математическое выражение в С++
    // int a, b, f;
    // cout << "Enter numbers a, b and f:" << endl;
    // cout << "a=";
    // cin >> a;
    // cout << "b=";
    // cin >> b;
    // cout << "f=";
    // cin >> f;

    // cout << "Calculate by formula: x= (a + b - f / a) + f * a * a - (a + b)" << endl;
    // cout << "x=" << (a + b - f / a) + f * a * a - (a + b) << endl;

    // 3. Перевод в верхний регистр
    // -32 from lower to upper | +32 from upper to lower
    // char lower_char;
    // cout << "Enter a char in lowercase:" << endl;
    // cin >> lower_char;
    // cout << "Some char in uppercase: " << (lower_char -= 32) << endl;

    // 4. Перевод числа из метров в километры
    // float meters;
    // cout << "Enter a number in meters: ";
    // cin >> meters;
    // cout << meters << " meters will be " << (meters / 1000) << " kilometers" << endl;

    // 5. Преобразование значения типа int в char
    // int n[2][9] = {
    //     {201,203,203,203,203,203,203,203,187}, // массив  рисунка из символов таблицы ASCII
    //     {200,202,202,202,202,202,202,202,188}
    // };
    
    // for(int i = 0; i < 2; i++) {
    //     for(int j = 0;j < 9; j++) {
    //         cout<<static_cast<char>(n[i][j]);
    //     }

    //     cout<<endl;
    // };
   
    // cin.get(); // строчно-ориентированная функция(метод) объекта cin, чтобы заморозить экран вывода

    // 6. Программно нарисовать домик в С++
    // cout << "House: \n\n";
    // cout << "\t     /\\ \n";
    // cout << "\t    /  \\ \n";
    // cout << "\t   /    \\ \n";
    // cout << "\t  /------\\ \n";
    // cout << "\t / |    | \\ \n";
    // cout << "\t   |    |  \n";
    // cout << "\t   |    |  \n";
    // cout << "\t   |____|  \n";

    // 7. Прямоугольный треугольник в C++
    // cout << "+" << endl << "++" << endl << "+++" << endl << "++++" << endl << "+++++" << endl << "++++++" << endl << "+++++++" << endl;

    // 8. Рамка на титул

    return 0;
}